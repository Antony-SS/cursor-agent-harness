# Architecture

Per-project. Describe **how this codebase is organized** and **how the important pieces interact**—only at the depth that helps. A tiny repo might be a short paragraph; a larger system might use extra headings. **Omit** topics that do not apply.

## Summary

_(Start here: what the project is, main entry points or apps, anything a newcomer must know.)_

_Add more `##` sections below only when they earn their keep. Rename or drop freely._

**Sometimes useful (pick any, skip the rest):**

- **Components or modules** — bounded parts and what each owns  
- **Data and external systems** — storage, APIs, trust boundaries (no secrets)  
- **How it runs** — local dev, deploy shape, notable config  
- **Repository layout** — rough paths only; discover from the filesystem, do not invent folders  

**See also:** `/docs/spec.md`, `/docs/tech_stack.md`, `/docs/features/{feature}/`.
